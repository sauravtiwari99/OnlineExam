import { TimeGeneratorPipe } from './time-generator.pipe';

describe('TimeGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
